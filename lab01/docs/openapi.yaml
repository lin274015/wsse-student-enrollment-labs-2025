openapi: "3.0.0"
info:
  title: "Student API"
  version: "1.0.0"
paths:
  /health:
    get:
      summary: "Health check"
      responses:
        "200":
          description: "OK"
          content:
            text/plain:
              schema:
                type: string
